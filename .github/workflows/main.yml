name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up SSH Agent
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install frontend dependencies
        run: npm install

      - name: Build frontend
        run: npm run build

      - name: Install backend dependencies
        run: npm install --prefix server

      - name: Deploy to VPS
        env:
          SSH_HOST: 104.223.123.201
          SSH_USER: root
        run: |
          set -x
          
          ssh-keyscan -H $SSH_HOST >> ~/.ssh/known_hosts
          
          ssh $SSH_USER@$SSH_HOST "sudo mkdir -p /var/www/booknow.hair/html"
          
          echo "Deploying frontend..."
          scp -o StrictHostKeyChecking=no -r dist/* $SSH_USER@$SSH_HOST:/var/www/booknow.hair/html/

          echo "Deploying backend..."
          ssh $SSH_USER@$SSH_HOST "sudo mkdir -p /var/www/booknow.hair/server"
          scp -o StrictHostKeyChecking=no -r server/* $SSH_USER@$SSH_HOST:/var/www/booknow.hair/server/
          ssh -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST "cd /var/www/booknow.hair/server && npm install --production && pm2 restart booknow-hair-server || pm2 start server.js --name booknow-hair-server"

  configure_nginx:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - name: Set up SSH Agent
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Configure Nginx
        env:
          SSH_HOST: 104.223.123.201
          SSH_USER: root
        run: |
          set -x
          # Define Nginx configuration content and write to a temporary file
          cat > nginx_config_temp << 'EOF'
server {
    listen 80;
    server_name booknow.hair 104.223.123.201;

    # Reverse proxy for API requests to the Node.js backend.
    location /api/ {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Serve the static frontend files.
    location / {
        root /var/www/booknow.hair/html;
        index index.html index.htm;
        # This allows client-side routing (e.g., with React Router) to work correctly.
        try_files $uri $uri/ /index.html;
    }
}
EOF

          # Transfer the config file to the VPS
          scp -o StrictHostKeyChecking=no nginx_config_temp $SSH_USER@$SSH_HOST:/tmp/booknow-hair-nginx

          # Move the config file to the correct Nginx directory and enable it
          ssh $SSH_USER@$SSH_HOST "sudo mv /tmp/booknow-hair-nginx /etc/nginx/sites-available/booknow-hair"
          ssh $SSH_USER@$SSH_HOST "sudo ln -sf /etc/nginx/sites-available/booknow-hair /etc/nginx/sites-enabled/booknow-hair"
          ssh -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST "sudo nginx -t && sudo systemctl reload nginx"
